info:
  name: Reset Password
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5000/api/auth/reset-password
  body:
    type: json
    data: |
      {
        "email": "test@test.com",
        "otp": "970792",
        "newPassword": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
