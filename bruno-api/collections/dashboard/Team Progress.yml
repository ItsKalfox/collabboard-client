info:
  name: Team Progress
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/dashboard/teams
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
