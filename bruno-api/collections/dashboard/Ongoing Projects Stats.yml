info:
  name: Ongoing Projects Stats
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/dashboard/projects/ongoing
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
