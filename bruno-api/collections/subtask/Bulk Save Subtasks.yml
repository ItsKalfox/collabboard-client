info:
  name: Bulk Save Subtasks
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:5000/api/tasks/task_001/subtasks
  body:
    type: json
    data: |-
      {
        "subtasks": [
          {
            "id": "sub_101",
            "title": "Desktop navbar layout",
            "completed": true
          },
          {
            "title": "New bulk subtask",
            "completed": false
          }
        ]
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
