info:
  name: Get Project Tasks
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/projects/1/tasks
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
