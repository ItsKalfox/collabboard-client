info:
  name: Get Task Reviews
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/tasks/101/reviews
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
