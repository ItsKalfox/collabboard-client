info:
  name: Delete Task
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:5000/api/tasks/101
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
