info:
  name: Get Attachment by ID
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:5000/api/attachments/att_001
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
