info:
  name: Get Task
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/tasks/101
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
