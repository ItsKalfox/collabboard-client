info:
  name: Update Subtask
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:5000/api/subtasks/sub_101
  body:
    type: json
    data: |
      {
        "completed": true,
        "title": "Updated Title"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
