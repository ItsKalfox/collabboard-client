info:
  name: Delete Project
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:5000/api/projects/proj_001
  auth:
    type: bearer
    token: <JWT_TOKEN>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
