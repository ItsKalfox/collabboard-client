info:
  name: Upload Cover Image
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:5000/api/projects/proj_001/cover-image
  body:
    type: multipartForm
    data:
      - name: image
        value: ''
        type: file
  auth:
    type: bearer
    token: <JWT_TOKEN>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
