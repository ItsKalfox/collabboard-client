info:
  name: Add Attachment
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:5000/api/projects/proj_001/attachments
  body:
    type: multipartForm
    data:
      - name: file
        value: ''
        type: file
  auth:
    type: bearer
    token: <JWT_TOKEN>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
