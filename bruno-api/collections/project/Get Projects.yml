info:
  name: Get Projects
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/projects
  auth:
    type: bearer
    token: <JWT_TOKEN>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
