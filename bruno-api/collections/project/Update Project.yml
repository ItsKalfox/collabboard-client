info:
  name: Update Project
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:5000/api/projects/proj_001
  body:
    type: json
    data: |
      {
        "name": "Updated Website Design",
        "description": "Updated description for the project",
        "status": "active"
      }
  auth:
    type: bearer
    token: <JWT_TOKEN>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
