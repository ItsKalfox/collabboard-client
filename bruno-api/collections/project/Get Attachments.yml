info:
  name: Get Attachments
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/projects/proj_001/attachments
  auth:
    type: bearer
    token: <JWT_TOKEN>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
