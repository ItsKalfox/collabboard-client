info:
  name: Get Project
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/projects/proj_001
  auth:
    type: bearer
    token: <JWT_TOKEN>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
